#include <stdio.h>
#include "main.h"
/**
 * main - function prints the name of the program
 * @argc:the number of arguments
 * @argv: the array comtains the arguments
 * Return: Always 0 if succes
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
